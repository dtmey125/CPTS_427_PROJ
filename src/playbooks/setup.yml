- name: Base setup
  hosts: all
  become: true
  tasks:
    # install iproute, if not already on machine.
    - name: Install iproute2
      apt:
        name: iproute2
        state: present
        update_cache: yes

# Set up Nagios on server
- import_playbook: nagios_server.yml
# set up Nagios on client
- import_playbook: nagios_clients.yml
